import { PaginationArrow } from "../Pagination.types";

/** Maps an arrow direction to its Material Symbol icon name. */
export const ARROW_ICON: Record<PaginationArrow, string> = {
  left: "arrow_back",
  right: "arrow_forward",
};
