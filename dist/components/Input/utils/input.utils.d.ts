/**
 * Builds the CSS class string for the input content wrapper,
 * applying state-based modifier classes when needed.
 */
export declare function getInputContentClass(disabled: boolean, error: boolean): string;
