/**
 * Tracks the `offsetWidth` of `.header-root` and updates on resize.
 * Used by `SavebarTrigger` to pin the SaveBar width to the header container.
 */
export declare function useHeaderWidth(): number | null;
