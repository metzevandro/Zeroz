/**
 * Builds the CSS class string for the trigger button,
 * applying state-based modifier classes.
 */
export declare function getTriggerClass(hasValue: boolean, error: boolean, disabled: boolean): string;
