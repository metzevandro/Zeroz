/**
 * Builds the CSS class string for the switch wrapper,
 * applying state-based modifier classes when needed.
 */
export declare function getSwitchClassList(disabled: boolean, noEvents: boolean): string;
